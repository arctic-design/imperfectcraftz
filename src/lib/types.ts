export interface Product {
    id: number
    name: string
    price: number
    originalPrice: number
    discount: number
    imageUrl: string
    category: string
    colors: string[]
    sizes: string[]
    rating: number
    reviewCount: number
    isNew: boolean
    description: string
    features: string[]
    productId: string
  }
  
  